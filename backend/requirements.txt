# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3 

# --- Embeddings + vector store ---
sentence-transformers==3.3.1
chromadb==0.5.23

# --- PDF parsing ---
pypdf==5.1.0

# --- Generator (Phi-3-mini via Hugging Face Transformers) ---
# torch is intentionally unpinned to let pip pick the CPU/CUDA build for your platform.
# On Windows CPU-only:  pip install torch
# On CUDA 12.1:         pip install torch --index-url https://download.pytorch.org/whl/cu121
torch
transformers==4.46.3
accelerate==1.1.1

# --- Optional but recommended ---
python-multipart==0.0.19   # for FastAPI file uploads if you add /upload later

# --- Manual classifier (Option B: auto-routing) ---
# matplotlib used by train_classifier.py to save the loss-curve plot for the report.
# numpy is already a transitive dep of torch/sklearn; we pin it lightly via pip's resolver.
matplotlib==3.9.4

# --- Testing (dev only) ---
# The pytest suite in backend/tests/ stubs torch/transformers/chromadb when they
# are absent, so these are the only packages needed to run the tests:
#   cd backend && pytest
pytest==8.3.4
httpx==0.28.1   # required by fastapi.testclient
